PORT=5000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/budget_tracker
JWT_SECRET=your_jwt_secret_key

DB_NAME=budget_db
DB_USER=postgres
DB_PASS=postgres   # <--- Ensure this is a plain string
DB_HOST=localhost  # or "db" if using Docker
